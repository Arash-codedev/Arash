


func is_prime
	copy n : Int
	-> Bool
body
    for var i = 0 : Int; i * i <= n; i++
        if n % i == 0
            return false
    return true


func main
body
	for var i = 0 : Int; i < 100; i++
		if is_prime(i)
			printf('%d\n', i)
    puts('Hello world')
